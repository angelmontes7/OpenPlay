import { SafeAreaView } from "react-native-safe-area-context";
import { Text, View } from "react-native";

const Chat = () => {
    return (
        <SafeAreaView>
            <Text> Home </Text>
        </SafeAreaView>
    );
};

export default Chat;